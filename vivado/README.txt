Setup for a timing on a zedboard  Zynq 7020 speed grade -1 (sadly)

To run this: 
1. Open vivado
2. Make a new project : test_dnn
3. Click on run implmentation
4. go to tcl window : source build_link.tc
5. Check to make sure the whole chain to bitstream is done
6. File > export > export hardware (export to ../sdk/)

